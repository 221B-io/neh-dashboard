{
  "name": "server",
  "version": "0.1.0",
  "description": "A server to use as a proxy for an ES instance.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "221B",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "bodybuilder": "^2.2.16",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "pug": "^2.0.3",
    "pug-plain-loader": "^1.0.0"
  }
}
